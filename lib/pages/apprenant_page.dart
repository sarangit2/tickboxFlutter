import 'package:flutter/material.dart';

class ApprenantPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Apprenant Dashboard'),
      ),
      body: Center(
        child: Text('Bienvenue, Apprenant!'),
      ),
    );
  }
}
