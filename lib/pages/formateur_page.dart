import 'package:flutter/material.dart';

class FormateurPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Formateur Dashboard'),
      ),
      body: Center(
        child: Text('Bienvenue, Formateur!'),
      ),
    );
  }
}
